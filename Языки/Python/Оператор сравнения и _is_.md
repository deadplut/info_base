3 == 3 - сравнивает значения двух [[Объект в Python|объектов]](true)
`==` - является синтаксическим сахаром метода `__eq__()`

```
a = 3
b = 3

# эквивалентные записи:
a == b
a.__eq__(b)
```

A is B - проверяет идентификатор объектов.
`is` быстрее опертора `==`, так как `==` можно перегрузить.


#is
#оператор_сравнения